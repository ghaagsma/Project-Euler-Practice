(() => {
    'use strict';

    // let factors = require('euler-common/src/factors');

    let getSolution = (target) => {
        return target;
    };

    module.exports = {
        getSolution: getSolution
    };
})();
