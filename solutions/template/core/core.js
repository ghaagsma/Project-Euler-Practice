(() => {
    'use strict';

    // let factors = require('javascript-utilities/src/factors');

    let getSolution = (target) => {
        return target;
    };

    module.exports = {
        getSolution: getSolution
    };
})();
